# Database
DATABASE_URL=
DIRECT_URL=

# Session
SESSION_SECRET=

# Web Push (VAPID) — generate with: npx web-push generate-vapid-keys
# Never commit real private keys.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# Stripe, email, etc. — see existing deployment docs
